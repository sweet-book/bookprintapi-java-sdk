package com.sweetbook.bookprintapi;

/** SDK 버전 상수. {@code build.gradle.kts}의 {@code version}과 동기화 유지. */
public final class SdkVersion {

    /** SDK 버전 (semver). 릴리즈 시 build.gradle.kts와 함께 갱신. */
    public static final String VERSION = "0.2.1";

    private SdkVersion() {}
}
